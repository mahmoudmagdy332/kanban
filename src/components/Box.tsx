import React from 'react'
import { box } from '../types/type'


const Box = ({cards}:box) => {
  return (
    <div className='w-full h-full overflow-y-scroll bg-[#bad1e0] flex-1'>

         {/* {cards.map((card)=>(
           
         ))} */}
     </div>
  )
}

export default Box